-- ============================================================
-- SiteOps Database Schema Update 005
-- DEPRECATED: Project zone handling merged into activity table
-- ============================================================

-- NOTE: The separate project_zone table has been consolidated into the activity table.
-- All zone/activity data is now managed through the activity table with the following columns:
-- - name (previously zoneName)
-- - lat (previously zoneLatitude)
-- - lng (previously zoneLongitude)
-- - posx, posy, widthpercent, heightpercent (layout positioning)
-- - displayorder (sort order)
-- - createdat, updatedat (timestamps)
-- - imagepath, imageurl (map images)
-- - markerlabel (marker label for maps)

-- This migration is kept for reference only.
-- The project_zone table structure (below) should not be created in new instances.
-- Use the activity table instead.

-- Legacy project_zone table (DEPRECATED - For reference only)
-- CREATE TABLE IF NOT EXISTS project_zone (
--     zoneid INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--     projectid INT NOT NULL REFERENCES project(projectid) ON DELETE CASCADE,
--     name VARCHAR(100) NOT NULL,
--     description VARCHAR(255) NULL,
--     activity VARCHAR(255) NULL,
--     status VARCHAR(50) NOT NULL DEFAULT 'pending',
--     progress INT NOT NULL DEFAULT 0,
--     lat DECIMAL(9, 6) NULL,
--     lng DECIMAL(9, 6) NULL,
--     markerlabel VARCHAR(100) NULL,
--     posx DECIMAL(5, 2) NULL,
--     posy DECIMAL(5, 2) NULL,
--     widthpercent DECIMAL(5, 2) NULL,
--     heightpercent DECIMAL(5, 2) NULL,
--     displayorder INT NULL,
--     createdat TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
--     updatedat TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
--     imagepath VARCHAR(255) NULL,
--     imageurl VARCHAR(255) NULL
-- );
